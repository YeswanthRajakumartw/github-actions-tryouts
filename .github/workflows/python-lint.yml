name: "python linting"
on: push
jobs:
  python-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Super-Linter
        uses: super-linter/super-linter@v5.2.0
