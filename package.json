{
  "name": "github-actions-tryouts",
  "version": "1.0.0",
  "description": "`Events` -- trigger --> `Workflows` -- uses --> `Actions`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}
